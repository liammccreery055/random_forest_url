# random_forest_url
a random forest model trained in python with sklearn and pandas. This model was trained to detect malicious URL's, its accuracy sits around 92%, but as of current i am working on breaking the data into a more digestable format for this model. 
